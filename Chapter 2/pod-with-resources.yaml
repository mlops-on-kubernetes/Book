apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
  - name: nginx-container
    image: nginx
    resources:
      limits:
        cpu: "500m"
        memory: "200Mi"
      requests:
        cpu: "200m"
        memory: "100Mi"
